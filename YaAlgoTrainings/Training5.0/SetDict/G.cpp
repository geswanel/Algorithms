#include <utility>
#include <iostream>
#include <unordered_set>

using namespace std;

using Point = pair<int, int>;

struct HashPoint {
    size_t operator()(const Point& p) const {
        return hash
    }
};

int main() {

    return 0;
}